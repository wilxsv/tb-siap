{% extends 'SonataAdminBundle:CRUD:list.html.twig' %}

{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript">
        jQuery(document).ready(function($) {
            /*
             * Declaracion de Variables
             */
            $idEstablecimiento = $('#filter_idEstablecimiento_value');
            $idModulo          = $('#filter_idModulo_value');
            $activo            = $('#filter_activo_value');

            var select2Options = {
                allowClear: true,
                dropdownAutoWidth: true,
                placeholder: 'Seleccionar...',
                containerCss: {
                    'width': '100%'
                }
            };

            /*
             * Inicializacion de Select2
             */
            select2Options['placeholder'] = 'Seleccionar Establecimiento...';
            initializeSelect2($idEstablecimiento, true, false, select2Options);

            select2Options['placeholder'] = 'Seleccionar Módulo...';
            initializeSelect2($idModulo, true, false, select2Options);

            select2Options['placeholder'] = 'Seleccionar...';
            initializeSelect2($activo, true, false, select2Options);
        });
    </script>
{% endblock %}
